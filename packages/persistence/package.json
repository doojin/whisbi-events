{
  "name": "@whisbi-events/persistence",
  "version": "1.0.0",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "lint": "eslint lib/**/*.ts",
    "test": "jest --runInBand"
  },
  "main": "dist/index.js",
  "dependencies": {
    "typeorm": "^0.2.31"
  }
}
