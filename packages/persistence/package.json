{
  "name": "@whisbi-events/persistence",
  "version": "1.0.0",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "lint": "eslint lib/**/*.ts",
    "test": "jest --silent"
  },
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/doojin/whisbi-events.git"
  },
  "dependencies": {
    "typeorm": "^0.2.31"
  }
}
