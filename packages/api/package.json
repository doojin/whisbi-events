{
  "name": "@whisbi-events/api",
  "version": "1.0.0",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "lint": "eslint lib/**/*.ts",
    "test": "jest --silent"
  },
  "main": "dist/index.js",
  "dependencies": {
    "@whisbi-events/persistence": "^1.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.11"
  }
}
