{
  "name": "@whisbi-events/web",
  "version": "1.0.0",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "lint": "eslint lib/**/*.ts",
    "test": "jest --runInBand"
  },
  "main": "dist/index.js",
  "dependencies": {
    "mysql": "^2.18.1",
    "typeorm": "^0.2.31"
  }
}
